import json
from classes.floors import Floor
from classes.rooms import Room

def save_and_exit(hotel):
    json_to_write = []
    for floor in floors.get_floors():
        if floor.get_floor():
            floor_json { 
                "floor_id": floor.get_id(),
                "room": {
                    "room_number": .get_room_number
                }
            }
        else:
            slot_json = {
                ""
            }